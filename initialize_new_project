to initialize a new project "npm init --y"
you will get node modules once you install a module like express npm i express

to start the db from data.sql you need to createdb books


and then psql
then \c books or psql -d books
then \i data.sql


to test:
npm install --save-dev jest supertest
 
and add a script

"scripts": {
  "test": "jest --coverage"
  },
  "jest": {
    "testEnvironment": "node"
  }